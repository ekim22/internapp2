import { InternshipInfoDirective } from './internship-info.directive';

describe('InternshipInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new InternshipInfoDirective();
    expect(directive).toBeTruthy();
  });
});
